{
  "name": "iptveditor4_bypass",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "bin": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js -o dist",
    "build:win": "npm run build && node scripts/create-executable.js win",
    "build:mac": "npm run build && node scripts/create-executable.js mac",
    "build:linux": "npm run build && node scripts/create-executable.js linux"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "axios": "^1.8.1",
    "fastify": "^5.2.1",
    "open": "^8.4.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
